{
  "name": "fruitystory-io",
  "version": "1.0.0",
  "private": true,
  "description": "FruityStory.io — fruit drama creation platform",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=main.js --format=esm"
  },
  "dependencies": {
    "@netlify/functions": "^4.2.5",
    "openai": "^5.12.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
